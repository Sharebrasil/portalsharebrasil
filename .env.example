# Example environment variables (DO NOT COMMIT real secrets)
# Copy this file to .env and fill values locally. Add .env to .gitignore.

# Supabase project URL (example: https://xyzabc.supabase.co)
SUPABASE_URL="https://your-project.supabase.co"

# Supabase service role key (keep secret, server-side only)
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# JWT secret used by server endpoints (if applicable)
JWT_SECRET="your-jwt-secret"

# Database URL for server (Neon/Postgres)
DATABASE_URL="postgresql://user:pass@host:5432/dbname"
